<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title>Snippet</title>
  <link rel="stylesheet" href="https://cdn.shoelace.style/1.0.0-beta22/shoelace.css">
  <link rel="stylesheet" type="text/css" href="/static/styles.css" />
</head>

<body>
  {{#snippet}}
<br />
    <h1 class="text-bold">{{title}}</h1>
    <div>
      <h3>Language: {{language}}</h3>
      <pre>{{body}}</pre>
      <blockquote>Notes: </strong>{{notes}}</blockquote>
      {{#tags}}
        <h3><a href="/tag/{{.}}">{{.}}</a> </h3>
      {{/tags}}
    </div>
  </br>
    <div class="links">
      <form action='/{{_id}}/edit/' method="GET">
        <button class="button-sm button-secondary">Edit this snippet</button>
      </form>
      <form action="/new_snippet/" method="GET">
        <button class="button-sm button-secondary button-info">Add a new snippet</button>
      </form>
      <form action="/{{_id}}/delete" method="POST">
        <button id="delete" type="submit" class="button-warning button-sm">Delete this record</button>
      </form>
      <form action="/" method="GET">
        <button href='/' class="button-sm button-success">Home Page</button>
      </form>

    </div>
  {{/snippet}}
  <script src="https://cdn.shoelace.style/1.0.0-beta22/shoelace.js"></script>
</body>

</html>
